<Window
    x:Class="KoMDViewer.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KoMDViewer">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header: Title Bar + Menu Bar -->
        <Border BorderThickness="0,0,0,1"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}">
            <StackPanel>
                <!-- Custom Title Bar -->
                <Grid x:Name="AppTitleBar" Height="36">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Image x:Name="TitleBarIcon" Width="20" Height="20"
                           Margin="12,0,0,0" VerticalAlignment="Center" Grid.Column="0"/>
                    <TextBlock x:Name="TitleText" Text="KoMDViewer"
                               VerticalAlignment="Center" Margin="8,0,0,0"
                               FontSize="12" FontFamily="Segoe UI"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Grid.Column="1"/>
                </Grid>

                <!-- Menu Bar -->
                <MenuBar>
                    <MenuBarItem Title="ファイル">
                        <MenuFlyoutItem Text="開く..." Click="OpenFile_Click">
                            <MenuFlyoutItem.KeyboardAccelerators>
                                <KeyboardAccelerator Modifiers="Control" Key="O"/>
                            </MenuFlyoutItem.KeyboardAccelerators>
                        </MenuFlyoutItem>
                        <MenuFlyoutSubItem x:Name="RecentFilesMenu" Text="最近のファイル"/>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem x:Name="SaveMenuItem" Text="保存" Click="SaveFile_Click" IsEnabled="False">
                            <MenuFlyoutItem.KeyboardAccelerators>
                                <KeyboardAccelerator Modifiers="Control" Key="S"/>
                            </MenuFlyoutItem.KeyboardAccelerators>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem Text="PDFに出力..." Click="ExportPdf_Click">
                            <MenuFlyoutItem.KeyboardAccelerators>
                                <KeyboardAccelerator Modifiers="Control,Shift" Key="E"/>
                            </MenuFlyoutItem.KeyboardAccelerators>
                        </MenuFlyoutItem>
                    </MenuBarItem>
                    <MenuBarItem Title="表示">
                        <MenuFlyoutItem x:Name="ThemeToggleItem" Text="ダークモード" Click="ToggleTheme_Click"/>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem x:Name="EditModeItem" Text="編集モード" Click="ToggleEditMode_Click" IsEnabled="False">
                            <MenuFlyoutItem.KeyboardAccelerators>
                                <KeyboardAccelerator Key="F2"/>
                            </MenuFlyoutItem.KeyboardAccelerators>
                        </MenuFlyoutItem>
                    </MenuBarItem>
                    <MenuBarItem Title="ヘルプ">
                        <MenuFlyoutItem Text="バージョン情報..." Click="About_Click"/>
                    </MenuBarItem>
                </MenuBar>
            </StackPanel>
        </Border>

        <!-- Content area: WebView + Loading + Drop overlay -->
        <Grid Grid.Row="1"
              AllowDrop="True"
              DragOver="DropTarget_DragOver"
              DragLeave="DropTarget_DragLeave"
              Drop="DropTarget_Drop">

            <WebView2 x:Name="WebView" DefaultBackgroundColor="Transparent"
                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

            <!-- Loading indicator (on top of WebView) -->
            <ProgressBar x:Name="LoadingBar" IsIndeterminate="True"
                         Visibility="Collapsed" VerticalAlignment="Top" HorizontalAlignment="Stretch"
                         Height="3" Margin="0"/>

            <!-- Drop visual feedback -->
            <Border x:Name="DropOverlay" CornerRadius="8" Margin="16" Visibility="Collapsed"
                    Background="{ThemeResource SystemFillColorAttentionBackgroundBrush}"
                    BorderBrush="{ThemeResource AccentFillColorDefaultBrush}" BorderThickness="2">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE8E5;" FontSize="48" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                    <TextBlock Text="Markdownファイルをドロップ" FontSize="16" Margin="0,12,0,0"
                               Foreground="{ThemeResource AccentFillColorDefaultBrush}" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" BorderThickness="0,1,0,0"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}">
            <StackPanel Orientation="Horizontal" Padding="12,5">
                <TextBlock x:Name="WordCountText" Text=""
                           VerticalAlignment="Center" FontSize="11" FontFamily="Segoe UI"
                           Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                <TextBlock x:Name="FilePathText" Text=""
                           VerticalAlignment="Center" FontSize="11" FontFamily="Segoe UI"
                           Foreground="{ThemeResource TextFillColorDisabledBrush}" Margin="16,0,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
