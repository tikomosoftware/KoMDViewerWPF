name: Release Build

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build-and-release:
    runs-on: windows-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '9.0.x'
    
    - name: Extract version from tag
      id: get_version
      shell: pwsh
      run: |
        $tag = "${{ github.ref_name }}"
        $version = $tag -replace '^v', ''
        echo "VERSION=$version" >> $env:GITHUB_OUTPUT
        echo "Version: $version"
    
    - name: Build Release Packages
      shell: pwsh
      run: |
        .\build-and-package.ps1 -Version "${{ steps.get_version.outputs.VERSION }}"
    
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          dist/KoMDViewer-v${{ steps.get_version.outputs.VERSION }}-release.zip
          dist/KoMDViewer-v${{ steps.get_version.outputs.VERSION }}-vector.zip
        body: |
          ## KoMDViewer v${{ steps.get_version.outputs.VERSION }}
          
          ### ダウンロード
          
          **通常版 (KoMDViewer-v${{ steps.get_version.outputs.VERSION }}-release.zip):**
          - 一般的な配布用パッケージです。
          - 必要環境: .NET 9.0 Desktop Runtime, Windows App SDK Runtime
          - [.NET 9.0 Runtimeをダウンロード](https://dotnet.microsoft.com/download/dotnet/9.0)
          
          **Vector用 (KoMDViewer-v${{ steps.get_version.outputs.VERSION }}-vector.zip):**
          - Vector等のソフトウェア配布サイト向けのパッケージです（READMEがVector用になっています）。
          
          ### 変更内容
          
          リリースノートをここに記載してください。
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
